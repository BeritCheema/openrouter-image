{
  "name": "openrouter-image",
  "version": "2.0.0",
  "description": "Generate and edit images with GPT Image 2 through OpenRouter",
  "type": "module",
  "scripts": {
    "generate": "node scripts/openrouter_image.js",
    "check": "node scripts/openrouter_image.js --help"
  },
  "engines": {
    "node": ">=18"
  }
}
